export default class GuessGame {
  constructor(name, answer, config, images, clues) {
    this.name = name;
    this.answer = answer;
    this.config = config;
    this.images = images;
    this.clues = clues;
  }
}
